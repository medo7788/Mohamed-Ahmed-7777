# حزمة تحديث ClevCalc Pro — الدفعة الأولى (النظام الأساسي)

هذه الحزمة فيها **ملفات جديدة/معدّلة فقط** بنفس مسارات المستودع بالظبط.
انسخها فوق نفس الأماكن في `Mohamed-Ahmed--main/` وارفعها على GitHub، وبعدها اضغط
التطبيق زي ما بتعمل عادي.

---

## 1) إصلاح حرج: أيقونة التطبيق كانت تالفة

`app/src/main/res/mipmap-*/ic_launcher.png` و`ic_launcher_round.png` (12 ملف) كانت
بايتات معطوبة فعليًا من جذورها (فحصتها بايت-بايت). استبدلتها بأيقونات PNG سليمة
بنفس تصميم الحاسبة الذهبية الموجود عندك في الـ vector الأصلي، بس دلوقتي صحيحة
ومفهومة لأي جهاز.

## 2) نظام أيقونات موحّد (Vector) بدل الإيموجي

ملف جديد: `ui/theme/IconMapping.kt`
بيربط كل أداة (CalcKey) وكل قسم (CategoryKey) بأيقونة Material Icons Extended
حقيقية — مكتبة already موجودة عندك في `build.gradle.kts`، يعني **صفر زيادة في حجم
التطبيق**، وشكل موحّد ثابت على كل الأجهزة بدل الإيموجي اللي بيختلف شكله من موبايل
لموبايل ومن Android لـ Android.

استخدمتها في:
- `ui/components/AppComponents.kt` (الهيدر، قائمة الثيمات، نافذة "حول التطبيق")
- `ui/screens/HomeScreen.kt` (كروت الأدوات، فلاتر الأقسام)

## 3) نظام تحديد موقع احترافي موحّد

كان عندك 3 نسخ مختلفة من كود تحديد الموقع مكررة في 3 شاشات (الصلاة، القبلة،
الطقس)، وواحدة منهم بتستخدم `getLastKnownLocation()` اللي بيرجع موقع قديم/غير
دقيق بدل قراءة حية.

ملفات جديدة:
- `util/AppLocationProvider.kt` — مصدر واحد لتحديد الموقع بدقة عالية (FusedLocationProviderClient) مع حالات واضحة (نجاح / إذن مرفوض / GPS مطفي / انتهت المهلة / خطأ).
- `ui/components/LocationStatusCard.kt` — ويدجت جاهز بيعرض حالة تحديد الموقع بشكل احترافي (زي تطبيقات الطقس والخرائط الكبيرة): أيقونة بتلف وقت البحث، دقة التحديد بالمتر، وزرار "سماح" أو "الإعدادات" لو المشكلة إذن أو GPS مطفي.

**ملحوظة مهمة:** الويدجت والمزوّد جاهزين ومربوطين بالثيم، لكن لسه محتاجين يتوصّلوا
بشاشات الصلاة/القبلة/الطقس الحالية (اللي فيها الكود القديم) — ده الخطوة الجاية
اللي هتلاقيها في قسم "المرحلة الجاية" تحت.

## 4) صوت الأذان + الإشعارات + الاهتزاز (شاشة جديدة كاملة)

قبل كده: التطبيق كان بيشغّل نغمة المنبّه الافتراضية بس، وبيهزّ الموبايل دايمًا من
غير ما تقدر توقفه.

دلوقتي: `ui/screens/AdhanSettingsScreen.kt` (شاشة جديدة، مربوطة في `MainActivity.kt`
تحت `CalcKey.ADHAN_SETTINGS` في قسم "إسلامية"):
- تفعيل/تعطيل الأذان لكل صلاة على حدة (نفس النظام القديم بتاعك، ما اتلمسش).
- **اختيار صوت الأذان** عن طريق منتقي الأصوات الرسمي بنظام أندرويد (نفس الطريقة
  اللي بتستخدمها Google Clock)، مع زرار تشغيل تجريبي قبل الحفظ.
- **تفعيل/تعطيل الاهتزاز** بشكل منفصل.
- `service/AdhanPlaybackService.kt` اتعدّل عشان يقرأ الاختيارين دول بدل القيم الثابتة.

## الملفات المطلوب حذفها (تقليل حجم التطبيق)

الملفات دي صور JPG تالفة أو نسخ يتيمة مش مستخدمة في أي مكان بالكود، وبتاخد مساحة
من غير أي فايدة:

```
app/src/main/res/drawable/clevcalc_logo.jpg                  (تالف)
app/src/main/res/drawable/clevcalc_gold_islamic_logo.jpg     (تالف)
app/src/main/res/drawable/clevcalc_gold_islamic_logo_1785285466342.jpg   (غير مستخدم)
app/src/main/res/drawable/clevcalc_logo_1785086781976.jpg               (غير مستخدم)
app/src/main/res/drawable/clevcalc_logo_1785284677776.jpg               (غير مستخدم)
app/src/main/res/drawable/app_logo_1785294303192.jpg                    (غير مستخدم)
```

احذفهم من المستودع مباشرة.

## المرحلة الجاية (لو عايز أكمل)

الطلب الأصلي كان يشمل كل أداة تفتح تبقى بشكل احترافي زي تطبيقات جوجل بلاي
الكبيرة. ده شغل حقيقي على 2000+ سطر تفصيلي في `IslamicScreens.kt` وملفات الأدوات
التانية، ومحتاج يترتب على دفعات عشان يطلع بجودة حقيقية مش سطحية:

1. ربط `LocationStatusCard` بشاشات الصلاة/القبلة/الطقس الحالية بدل الكود المكرر.
2. توحيد تصميم كل شاشة أداة (Scaffold موحّد بعنوان + أيقونة + بطاقة نتيجة، بدل ما
   كل شاشة تبني تصميمها من الصفر بمقاسات مختلفة).
3. مراجعة دالة تقييم التعبير في الآلة الحاسبة الأساسية.

قولّي تحب نبدأ بأنهي حاجة من دول وهكمل عليها.
